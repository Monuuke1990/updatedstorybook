import React from 'react';

function employee (props){
        return(<div>
                 <b>
                     hello {props.name}
                 </b>
        </div>);
    
}
export default employee;